import './Hero.css';

const Hero = () => {
    return (
        <section className="hero">
            <div className="hero-banner">
                <h2>Download Product Brochures</h2>
            </div>
        </section>
    );
};

export default Hero;